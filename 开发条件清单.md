# 项目开发条件清单（基于 xuqiu.md 整理）

> 用途：开发前核对就绪状态；开发中作为实现依据；验证与自我优化时作为检查清单。
> 需求源：`src/main/resources/xuqiu.md`（百度地图地址查询与格式化工具）

---

## 〇、开发完成状态（2026-09-12 更新，含准确性迭代记录）

代码已开发完成并通过验证；全量 1285 地址跑批 + 逐轮准确性迭代完成。

### 全量跑批结果（最终轮）
- 共 1285 条唯一地址：成功 1084 + 低置信度 199（合计 1283 行进 output.txt）+ 失败 2（failed.txt），账目平衡无静默丢失
- output.txt 全部 1283 行 5 字段完整；center 全部合法 JSON 且仅 1 条超验收框（机训大队，全部候选≤20 分已标低置信，百度无该 POI，无更优解）；address 全部合法 JSON、非空时 ≥3 顶点且首尾闭合；无多边形 502 条正确输出 `[]`
- 自测 45/45 通过（含 6 个错配回归用例）

### 准确性迭代记录（修改前后对比，均已固化为回归用例防回归）
| 轮次 | 问题（前） | 根因 | 修复（后） |
|---|---|---|---|
| 0 | 大剧院等查不到；广州市/柳州市跨市错配 | b 参数是 HAR 里 200 米小视野；请求无 Cookie；无候选过滤 | 视野改深圳全域；加 baidu.cookie；城市+边界守卫 |
| 1 | 保利花园4栋→坪山明玥澜岸；好望角B2栋→坪山"B2栋"；裕安一路西延→天健坪山 | 名称包含判定过松；缺子序列匹配；并列决胜"名称更长"被滥用 | 包含判定要求候选含≥2汉字；输入为候选名子序列给 28 分；决胜改按百度原始排序优先；新增片区→行政区锚定分 |
| 2 | 信义学校宿舍楼→异地"宿舍楼" | 通用名靠包含判定白拿 30 分；"栋宿舍"等泛词贡献关键词分 | 通用词停用表，不作匹配证据 |
| 3 | 水榭花都听水居3栋→13栋 | "13栋"包含子串"3栋"白拿楼栋分 | 单元号匹配加左边界（前一字符非数字/字母） |
| 4 | 南方国际广场→其地下停车场出入口；铲岛路A1-1→三津汤包 | "XX广场地下停车场"等设施名包含输入得满分；小区内商铺靠路名命中胜过楼栋 | 含通用词的候选名称封顶 15 分；住宅类输入遇商业业态候选名称/地址减半 |

### 浏览器人工比对（与百度地图网页逐条对比，8 条）
- 随机 5 条：水榭花都听水居3栋 ✓、南方国际广场 ✓、鹤洲新村4巷2号 ✓（与网页第一结果一致）、铲岛路A1-1 ✓（迭代4修复后选同址楼栋）、崇文花园12栋 ✓
- 重点场景：多楼栋合并 佳兆业中心A、B座 ✓（A/B 座子查询均正确，合并中心位于南园路866号园区内）；易混淆 香蜜楼B单元 ✓（百度无该子 POI，选母体香蜜楼，未误选其他小区）；无多边形地址正确输出 `[]` ✓
- 评分明细已全部打印到运行日志（每条含候选名称、五维得分、总分、选中项），低置信度另汇总于 review.txt

### 待用户操作
1. `html.ak` 换成自己的浏览器端 AK 后用 map.html 人工复核（重点看黄色低置信 199 条）；
2. 低置信度中确有错配的，可把正确 POI 名反馈给评分规则继续迭代。

---

## 一、结论速览

| 条件 | 状态 | 说明 |
|---|---|---|
| JDK 8 | ✅ 就绪 | 1.8.0_331（64-bit） |
| Maven | ⚠️ 已安装未入 PATH | `D:\java\maven\apache-maven-3.6.3`，本地仓库 `~/.m2/repository` 已存在；需加入 PATH 或配 IDE |
| pom.xml 依赖 | ❌ 待补 | 当前是空壳 POM，未声明任何依赖 |
| 源码骨架 | ❌ 待开发 | `Main.java` 仍是 IDEA 模板代码，`src/test/java` 为空 |
| 输入数据 dizhi.txt | ✅ 就绪 | 1289 行真实地址（含首行标题，见风险 R2） |
| 抓包样例 HAR | ✅ 就绪 | 含 1 次 `qt=s` + 1 次 `qt=usync` 完整请求/响应，响应结构与需求第六节一致 |
| 鉴权参数 auth/seckey | ✅ 已提取 | 抓包时间约 2026-09-11（很新），初始值见第三节；有过期风险（R1） |
| 网络访问 map.baidu.com | ⚠️ 待验证 | 开发机需能直连百度地图接口 |
| Maven 中央仓库访问 | ⚠️ 待验证 | 需能下载 jackson / jts / lombok |
| 鉴权自动刷新（Edge） | ⚠️ 待验证 | 需 Edge 以 `--remote-debugging-port` 启动（R8），CDP 连通后即可自动取新 key |
| 离线复核能力 | 📋 设计已定 | 响应全量落盘 cache + 离线重放模式 + map.html 复核状态着色（3.5），开发时按此实现 |

---

## 二、环境条件

1. **JDK 8**：已就绪。禁止使用 Java 11+ API（需求第十二节硬性约束）。
2. **Maven**：`D:\java\maven\apache-maven-3.6.3`（bin 未加入 PATH）。任选其一：
   - 将 `D:\java\maven\apache-maven-3.6.3\bin` 加入 PATH；
   - 或在 IDEA 中指定 Maven home 为该目录。
3. **运行方式**：控制台程序，`mvn compile exec:java` 或打包后 `java -jar`（POM 需补 `exec-maven-plugin` 或 `maven-shade-plugin`，属于开发时的技术选型，需求未指定，建议 shade 打可执行 fat-jar）。
4. **输出目录**：程序工作目录下生成 `output.txt`、`map.html`（路径可配）。

## 三、外部依赖：百度地图接口（最关键的外部条件）

### 3.1 鉴权参数初始值（用户提供，可直接写入 config.properties）

参数会过期。**过期处理已升级为自动化**：程序检测到失效后，自动调用用户已打开的 Edge 浏览器重新抓取新 key（见 3.4），无需人工重新抓包。

```properties
baidu.auth=【占位：真实值只保存在本地未跟踪的 ./config.properties，不入库】
baidu.seckey=【占位：同上】
baidu.validate=34993
baidu.cityCode=340
```

注意：同一会话内 `auth` 值会随请求变化，配置中保存最近一次有效值即可；`BaiduAuthManager` 负责失效检测与刷新调度。

### 3.2 请求规范（开发依据）

- **推荐简化方案（需求 3.4）**：只实现 `qt=s` GET 搜索请求；`usync` POST 流程作为备用，仅当 GET 稳定失败时补充。
- 请求头必须带：`User-Agent`（浏览器 UA）、`Referer: https://map.baidu.com/`。
- 超时：连接 10s、读取 20s；失败重试 2 次。
- 请求间隔 200~500ms（配置项 `request.delay.ms`，默认 300）。
- `wd` 需 UTF-8 URL 编码；`t` 用当前毫秒时间戳。
- 失效判定：`result.error != 0` 或 `content` 为空 → 提示重新抓包更新 auth/seckey。
- **响应字段已用 HAR 实测确认存在**：`content[]`、`name`、`poi_address`、`std_tag`、`ext.detail_info.navi_xy.diPoint`、`ext.detail_info.guoke_geo_bud.bud_geom`（`POLYGON ((x y, ...))` 分号分隔多个多边形）。

### 3.3 HTML 展示依赖

`map.html` 使用百度地图 JS API 3.0，需要一个有效的**浏览器端 AK 密钥**（占位符 `【你的AK】`），需自行申请（lbsyun.baidu.com），这是独立于服务端 auth 的另一个密钥。

### 3.4 鉴权自动刷新：调用已打开的 Edge 浏览器（新增需求条件）

当接口返回 `result.error != 0` 或 `content` 持续为空（判定为鉴权失效）时，程序自动从用户**已打开的 Edge 浏览器**中获取新的 auth/seckey，流程：

1. 程序停止请求 → 触发 `AuthRefresher`（新模块）。
2. 通过 **CDP（Chrome DevTools Protocol）** 连接 Edge：Edge 必须以 `--remote-debugging-port=9222` 启动才能被外部程序接管（见风险 R8）。
3. 用 Edge 打开 `https://map.baidu.com/`，在搜索框输入一个测试地址（如"领秀花园E栋"）并触发搜索。
4. 监听 CDP 的 `Network.requestWillBeSent` 事件，捕获 `qt=s` 请求，从 URL 中提取最新的 `auth`、`seckey`（及 `validate`，如有）。
5. 更新 `BaiduAuthManager` 内存值并**回写 config.properties**（保证程序重启后仍有可用 key），然后继续被中断的地址队列。

技术选型：Java 8 下可用 selenium-java（需对应版本 msedgedriver），或直接用 HTTP + WebSocket 对接 Edge 的 CDP 端口（无额外驱动依赖，推荐）。刷新成功/失败都必须有明确日志；刷新失败时保留旧 key 继续重试并提示用户。

### 3.5 离线复核能力（新增需求条件）

用户要求：**项目运行停止后，仍能离线复核点位与区域边界的准确性，并能通过 map.html 快速人工校验**。为此程序必须落盘所有中间数据：

1. **原始响应缓存**：每个地址的百度接口原始 JSON 按行存入 `cache/` 目录（文件名用地址哈希或行号+地址），作为可重放数据源。
2. **离线重放模式**：程序支持 `offline=true` 配置——不联网，直接读 `cache/` 重走"解析→坐标转换→多边形筛选→择优→合并→输出"链路，重新生成 `output.txt` 和 `map.html`。这样调参（评分阈值、拆分规则、坐标转换算法）后无需重新请求百度。
3. **失败与低置信度清单**：生成 `failed.txt`（失败地址+原因）和 `review.txt`（低置信度地址+评分明细），复核有明确目标。
4. **map.html 复核增强**：每条数据内嵌复核状态（成功/低置信/失败、评分明细、原始输入地址），地图上用颜色区分（绿=成功、黄=低置信、红=失败）；提供图例和按状态过滤的控件；点击可查看该地址的输入串→选中候选名称→评分明细，便于快速判断"点位/边界是否取对了"。

## 四、Maven 依赖（pom.xml 待补）

```xml
<dependency>
    <groupId>com.fasterxml.jackson.core</groupId>
    <artifactId>jackson-databind</artifactId>
    <version>2.15.4</version>
</dependency>
<dependency>
    <groupId>org.locationtech.jts</groupId>
    <artifactId>jts-core</artifactId>
    <version>1.19.0</version>
</dependency>
<dependency>  <!-- 可选 -->
    <groupId>org.projectlombok</groupId>
    <artifactId>lombok</artifactId>
    <version>1.18.30</version>
    <scope>provided</scope>
</dependency>
<!-- 测试 -->
<dependency>
    <groupId>junit</groupId>
    <artifactId>junit</artifactId>
    <version>4.13.2</version>
    <scope>test</scope>
</dependency>
```

## 五、配置项清单（config.properties，需求第十二节）

| 配置项 | 默认值 | 用途 |
|---|---|---|
| `baidu.auth` / `baidu.seckey` / `baidu.validate` | 见 3.1 | 接口鉴权 |
| `baidu.cityCode` | 340 | 深圳城市代码 |
| `baidu.autoRefresh` | true | 鉴权失效时是否自动调 Edge 刷新（3.4） |
| `edge.debugPort` | 9222 | Edge CDP 调试端口 |
| `output.path` | output.txt | 结果输出 |
| `html.path` | map.html | 地图展示 |
| `cache.dir` | cache | 原始响应缓存目录（离线复核用，3.5） |
| `offline` | false | 离线重放模式：只读 cache 不联网 |
| `region.id.start` | 10124 | region_id 起始自增值 |
| `request.delay.ms` | 300 | 请求间隔 |
| `match.score.threshold` | 40 | 择优最低置信分 |

另建议增加（自我优化用）：`input.path`（默认 dizhi.txt）、`skip.below.threshold`（低于阈值是否跳过，需求 5.4 允许配置）。

## 六、输入数据条件（dizhi.txt 实测特征）

- 共 1289 行地址，覆盖福田/南山/宝安/龙华等区，绝大多数为深圳（cityCode=340 适用）。
- **多楼栋地址的写法远比需求第四章的规则复杂**，拆分器测试用例应覆盖以下真实样本：
  - 顿号列表：`佳兆业中心A、B座`、`统建楼办公楼1栋、2栋、3栋、4栋`
  - 无顿号连写：`韵动家园AB栋`、`香江西苑AB座`、`天健名苑AB栋`、`龙珠军苑A,B栋`（半角逗号）
  - 斜杠：`中海华庭华景峰1/2座`、`名仕春天A/C座`
  - 范围：`水榭花都润雨居1-5单元`、`前海天境花园1-4栋`
  - 括号别名：`天元·花园1栋（T1）`、`香蜜湖第一生态苑华府2栋（华日阁、华东阁）`
  - 枚举号段：`香蜜湖第一生态苑御园14处（69-78号）69/70/71/72/73/75/76/78`
  - 复合：`安华工业区9栋A单元、8栋、7栋`
- 不可拆分时按需求退化为整串查询取第一结果。

## 七、模块清单与开发顺序（需求第十一、十四节）

```
com.fh（沿用现有 groupId）
├── Main.java                    入口
├── config/AppConfig.java        读取 config.properties
├── service/
│   ├── BaiduMapService.java     ① 先做：GET 搜索 + 打印原始 JSON + 响应落盘 cache/
│   ├── BaiduAuthManager.java    ② 鉴权参数管理、失效检测、回写配置
│   ├── AuthRefresher.java       ②b CDP 连接 Edge 自动抓取新 key（3.4，新增）
│   ├── OfflineReplayService.java ⑧ 离线重放：读 cache 重建 output/map（3.5，新增）
│   ├── AddressSplitter.java     ⑥ 多楼栋拆分
│   └── AddressMatcher.java      ⑤ 多结果评分择优（40/25/20/10/5 权重）
├── util/
│   ├── CoordinateConverter.java ③ BD-09MC → BD-09 经纬度
│   ├── PolygonUtils.java        ③ 射线法 + 多边形筛选
│   ├── PolygonMerger.java       ⑥ 凸包合并（JTS union 备选）
│   └── OutputFormatter.java     ⑦ output.txt + map.html 生成
└── model/AddressResult.java
```

顺序：接口调用 → JSON 解析 → 坐标转换/多边形筛选 → 评分择优 → 拆分/合并 → 格式化输出 → map.html。

## 八、验证方案（开发与自我优化均按此执行）

### 8.1 离线单测（不依赖网络，先做）
1. **JSON 解析**：把 HAR 中 `qt=s` 的响应体保存为 fixture 文件，验证字段提取（中心点、bud_geom、std_tag）。
2. **坐标转换**：用 HAR 中的墨卡托坐标转换后，与请求 `b=(12674865...,2569320...)` 视野及已知地标经纬度对照，误差应在米级内（见风险 R3）。
3. **射线法**：构造凹/凸多边形与内外点用例（含边界点）。
4. **拆分器**：用第六节列出的真实样本逐一断言拆分结果。
5. **评分器**：用需求 5.3 示例（`香蜜楼B单元`，4 个候选）断言 `香蜜楼B单元` 得 100 分胜出；补充并列决胜、低于阈值用例。
6. **凸包合并**：两个分离矩形合并后验证凸包顶点数与中心点位置。

### 8.2 在线联调
1. 先单查 1 个地址（如 `领秀花园E栋`，HAR 中有基准响应可对照），确认 auth 有效、返回结构一致。
2. 取 dizhi.txt 前 10 行（含多楼栋与易混淆地址）小批量跑通全链路。
3. **鉴权自动刷新演练**：故意把 config 中的 auth 改成无效值 → 程序应自动唤起 Edge 抓到新 key 并恢复请求。
4. **准确性抽验（最终目标：点位和区域边界准确）**：随机抽 20~30 条成功结果，与百度地图网页版人工对照——中心点是否落在楼栋上、多边形是否就是该楼栋轮廓；重点抽多楼栋合并（`佳兆业中心A、B座`）和多候选地址（`香蜜楼B单元`）。
5. 全量 1289 行前估算耗时：约 1300+ 次请求（多楼栋放大），间隔 300ms → 至少 15~30 分钟，建议支持断点续跑/失败清单输出（自我优化项）。

### 8.3 输出校验
- `output.txt`：tab 分隔 5 列；region_id 从 10124 自增；经纬度 14 位小数；多边形首尾闭合；`place_type` 缺省为"未知"；空多边形输出 `[]`。
- 控制台：`[成功]/[失败]` 进度行 + 择优评分明细。
- `map.html`：浏览器打开，校验 AK 有效、中心点标记、多边形渲染、信息窗口、视野自适应；按复核状态着色（绿/黄/红）、图例与过滤可用；点击可见输入地址→选中候选→评分明细的追溯链。

### 8.4 离线复核演练（新增，程序停止后执行）
1. 断网（或改 `offline=true`），基于 `cache/` 重放生成 output.txt/map.html，结果应与在线运行一致。
2. 修改评分阈值/拆分规则后重放，验证调参只影响择优结果而不需要重新联网。
3. 用 map.html 按"低置信/失败"过滤，逐条人工复核，确认可通过复核结果反推规则调整。

### 8.5 自我优化指标
- 成功率（成功行/总行）、低置信度行占比、鉴权失效自动识别与 Edge 刷新成功率、重复地址去重（dizhi.txt 存在完全重复行，如 `阳光棕榈园31栋` 出现两次）、离线重放结果与在线结果的一致率。

## 九、风险与待确认事项

| # | 风险 | 说明 | 建议 |
|---|---|---|---|
| R1 | auth/seckey 过期 | 用户提供的新 key（3.1），时效未知 | 已有自动化方案：失效时经 Edge CDP 自动刷新并回写配置（3.4） |
| R2 | dizhi.txt 首行为标题 | 第 1 行 `具体位置描述` 非 `#` 开头、非空，按现规则会被当作地址查询 | 在拆分/读取层将其过滤或确认删除 |
| R3 | 坐标转换公式可能不准 | 需求第七节给出的是**标准 Web Mercator** 逆变换；百度墨卡托（BD-09MC）基于百度自有投影，与标准 Web Mercator 存在偏移 | 用已知地标实测校验；若偏移，改用百度官方 MC2LL 迭代算法（GCJ02 中转）。**这直接决定"点位和边界准确"这一最终目标，优先验证** |
| R4 | 限流/封禁 | 1300+ 次请求 | 间隔可配并支持加长；失败重试 2 次后跳过并记录失败清单 |
| R5 | 数据笔误 | `莲华一村16栋`（同小区其余为"莲花一村"） | 依赖评分器的地址关键词维度自然降权，无需特判，但需人工复核输出 |
| R6 | 大地址量HTML | 1000+ 多边形内嵌 HTML 可能较大 | 顶点坐标保留 14 位小数即可；若文件过大可抽稀顶点 |
| R7 | AK 未申请 | map.html 需浏览器端 AK | 开发前到百度地图开放平台申请浏览器端 AK |
| R8 | Edge CDP 接管限制 | **只有以 `--remote-debugging-port=9222` 启动的 Edge 才能被 CDP 接管**；用户日常使用的 Edge 若非如此启动，程序无法连接"已打开的"实例 | ① 提示用户用带调试端口的快捷方式启动 Edge；或 ② 程序自行以调试端口拉起独立 Edge 实例（复用用户 profile 可免登录态问题，但 profile 被占用时需 `-user-data-dir` 副本）；两种方式都要在 README 写清 |
| R9 | 离线复核依赖缓存完整 | cache 缺失（如首次运行中断前未落盘）会导致重放不完整 | 每个响应**先落盘再解析**；提供 cache 完整性校验（行数对账） |

## 十、立即可执行的启动动作

1. 将 `D:\java\maven\apache-maven-3.6.3\bin` 加入 PATH（或配置 IDEA Maven home）。
2. 按第四节补全 pom.xml 依赖，`mvn compile` 验证依赖可下载。
3. 按 3.1 生成 `src/main/resources/config.properties`（填入用户提供的最新 auth/seckey/validate）。
4. 保存 HAR 中 `qt=s` 响应为测试 fixture，开始 8.1 离线单测；**优先验证 R3 坐标转换准确性**（最终目标的第一道关口）。
5. 验证 Edge CDP 连通性：以 `--remote-debugging-port=9222` 启动 Edge，用程序/浏览器访问 `http://127.0.0.1:9222/json` 确认可接管（3.4 前置条件）。
6. 按 3.3 申请浏览器端 AK（可与开发并行）。
